# Техническая документация: Converter

## 1. Архитектура высокого уровня
- **Form1** (частичный класс `Form1.cs` + `Form1.UI.cs`) — главный WinForms экран. Здесь создаётся весь UI, настраиваются обработчики, orchestrator очереди, оценка и запуск FFmpeg.
- **UI/** — специализированные контролы (плитки, панели редактора, диалоги), которые подключаются к форме.
- **Services/** — чистые службы (PresetService, QueueManager, EstimationService, ThumbnailService, NotificationService, AudioProcessingService, ShareService, ThemeManager и др.), скрывающие работу с FFmpeg, файловой системой и Windows API.
- **Models/** — DTO для очереди, пресетов, тем, уведомлений, отчётов и оценок.

Диаграмма потоков:
1. Пользователь добавляет файлы → `DragDropPanel` и `FileListItem` уведомляют `Form1`.
2. `Form1` зондирует файлы (через `ThumbnailService`/`EstimationService`) и создаёт `QueueItem`.
3. `QueueManager` последовательно/параллельно вызывает `ConvertQueueItemAsync`, который формирует и запускает FFmpeg через Xabe.FFmpeg.
4. По окончании запускаются `NotificationService`, `ShareService`, обновление истории и UI.

## 2. Компоновка интерфейса
- **Левая колонка**: верхний тулбар (кнопки добавления, удаления, очистки, шаринга, открытия редактора), далее `DragDropPanel` и поток `FileListItem` с подробностями и прогрессом.
- **Правая колонка**: TabControl с вкладками:
  - `⭐ Пресеты`: плитка `PresetPanel`, собираемая из `PresetService` (в т.ч. XML/JSON пресеты); кнопки мгновенно настраивают UI.
  - `🎬 Видео`: формат, кодеки, качество (CRF), масштабирование (пресеты/проценты), шаблоны имён, опции вывода, путь к FFmpeg, потоки, аппаратное ускорение.
  - `🔊 Аудио`: включение/отключение дорожки, кодек, битрейт, панель расширенной аудиообработки.
  - `📋 Очередь`: `QueueControlPanel` (автостарт, параллелизм, фильтры, сортировки, очистка завершённых) + список `QueueItemControl`.
- **Нижняя панель**: сплиттер "прогресс + лог". В секции прогресса находятся два ProgressBar, статусные Label, `EstimatePanel`, кнопки Старт/Стоп, сохранение/загрузка пресетов и настройка уведомлений.
- **Тема**: при первом открытии `InitializeAdvancedTheming()` включает `ThemeSelectorControl` и подписывает форму на изменения `ThemeManager`.

## 3. Импорт и метаданные
- `DragDropPanel` принимает файлы из диалога или drag&drop, фильтрует по расширениям и создаёт `VideoThumbnailControl`.
- `VideoThumbnailControl` вызывает `FFmpeg.GetMediaInfo`, отображает длительность/кодек и генерирует миниатюру (кэшируется через `ThumbnailService`).
- `FileListItem` хранит путь, показывает миниатюру, размер, тип и прогресс; имеет контекстное меню для обновления превью.
- `ThumbnailService` поддерживает in-memory и диск-кэш, использует FFmpeg для генерации кадров, ограничивает число параллельных задач.

## 4. Настройки кодирования
- Стандартные параметры (формат, кодеки, CRF) считываются из комбобоксов, масштабирование переключается между пресетом высоты и процентами.
- `PresetService` объединяет встроенные профили и XML файлы, обеспечивает сохранение/загрузку пресетов в JSON/XML.
- `AudioProcessingPanel` собирает `AudioProcessingOptions`, которые через `AudioProcessingService.BuildAudioFilterString` превращаются в цепочку FFmpeg фильтров (loudnorm, afftdn, equalizer, afade).
- `EstimationService` анализирует видео (через FFmpeg), оценивает исходный bitrate/bppf, учитывает CRF, масштаб, кодек, рассчитывает ожидаемый размер/экономию/время и обновляет `EstimatePanel` через debounce‑таймер.

## 5. Очередь и выполнение
- Добавленные файлы конвертируются в `QueueItem` (с полями FilePath, настройки, приоритет, статус, ETA).
- `QueueManager` держит ObservableCollection, управляет `CancellationTokenSource` на элемент, разрешает перемещение вверх/вниз, установку приоритета, звёздочки, отмену, очистку завершённых, изменение максимального числа параллельных заданий и стратегию автостарта/остановки при ошибке.
- Вкладка очереди отображает элементы через `QueueItemControl` и обновляет агрегаты (`QueueStatistics`). При завершении можно открыть `StatisticsDialog`.
- `ConvertQueueItemAsync` собирает итоговые параметры (формат, кодеки, CRF, битрейт, выходной путь), передаёт их в `ConvertFileAsync`.
- `ConvertFileAsync` создаёт Xabe-конверсию: задаёт вход, фильтры (`scale`, `af`), preset/threads/аппаратное ускорение, `-movflags +faststart`, `-c:v copy`/`-c:a copy` при прямом копировании, назначает обработчики `OnProgress`/`OnDataReceived`. По окончании обновляет историю `_conversionHistory` и очередь.
- Папка `Converted` создаётся автоматически при установленном чекбоксе, путь к FFmpeg подтягивается через `EnsureFfmpegAsync()` (загрузка через `FFmpegDownloader`).

## 6. Видео‑редактор
`VideoEditorForm` загружает видео в `VideoPlayerPanel` и предоставляет вкладки: SubtitlesEditorPanel (импорт/редактирование текста), CropPanel (рамка кадрирования), TrimPanel (обрезка с таймлайном), EffectsPanel (фильтры). Кнопка "👁 Предпросмотр" создаёт временный файл, кнопка "💾 Экспортировать" сохраняет итог.

## 7. Уведомления, шаринг и аналитика
- `NotificationService` использует Microsoft.Toolkit.Uwp.Notifications для построения toast'ов с текстом, inline‑картинкой, кнопкой "Открыть папку" и настраиваемым звуком. Есть уведомления о промежуточных стадиях (25/50/75%) и диалог `NotificationSettingsForm` для включения/тестирования.
- `ShareService` и `ShareDialog` формируют отчёт (кол-во файлов, экономия места, время, популярные кодеки, пресеты) и дают текстовые заготовки для соцсетей + генератор PNG карточки.
- `StatisticsDialog` визуализирует агрегированные метрики (total/completed/failed, input/output size, saved bytes, средняя скорость).

## 8. Темизация
- `ThemeManager` хранит палитры (`Theme`), поддерживает анимированные переходы, авто‑переключение по расписанию, сохранение настроек и рассылку события `ThemeChanged`. `ThemeSelectorControl` предоставляет комбо‑бокс выбора темы, переключатели авто‑режима и анимации, кнопку открытия `ThemeSettingsDialog`.
- При закрытии формы событие отписывается и `NotificationService` освобождает ресурсы.

## 9. Расширение
- Добавление новых пресетов — через `PresetService` (JSON или XML), UI автоматически перестроит плитку.
- Новые аудио/видео опции можно ввести, расширив `AudioProcessingOptions`/`ConversionSettings` и обновив конструктор команд в `ConvertFileAsync`.
- Очередь поддерживает замену обработчика: в `QueueManager` можно передать другой делегат вместо `ConvertQueueItemAsync` для тестов.
- ThemeManager хранит набор палитр в `Theme.GetAllThemes()`; можно добавить новую тему без изменений формы.
