# UI

Каталог `UI` содержит все пользовательские контролы, редакторы и диалоги, которые расширяют основную форму `Form1`.

## Основные панели
- **DragDropPanel** (`DragDropPanel.cs`) — плитка с миниатюрами, поддержкой drag&drop и выбором файлов.
- **AudioProcessingPanel** (`AudioProcessingPanel.cs`) — визуальный конфигуратор нормализации громкости, шумоподавления, эквалайзера и fade‑эффектов.
- **VideoPlayerPanel / VideoThumbnailControl** — переиспользуемые панели для предпросмотра, генерации миниатюр и отображения метаданных.
- **VideoEditorForm** (`VideoEditorForm.cs`) объединяет вкладки SubtitlesEditorPanel, CropPanel, TrimPanel и EffectsPanel для постобработки роликов.

## Контролы
- `Controls/FileListItem.cs` — карточка файла с миниатюрой, прогрессом и контекстным меню.
- `Controls/EstimatePanel.cs` — резюме оценок размера и времени.
- `Controls/QueueControlPanel.cs` и `Controls/QueueItemControl.cs` — управление очередью (старт/стоп/фильтры/приоритеты) и карточки элементов.
- `Controls/PresetPanel.cs` и `Controls/PresetButton.cs` — плитка с пресетами, группировка по категориям.
- `Controls/ThemeSelectorControl.cs` — селектор темы, переключение анимаций и авто‑режима.

## Диалоги
- `Dialogs/NotificationSettingsForm.cs` — конфигурация toast‑уведомлений.
- `Dialogs/StatisticsDialog.cs` — итоговая статистика очереди.
- `Dialogs/ShareDialog.cs` — предпросмотр и генерация текстов/картинки для социальных сетей.
- `Dialogs/ThemeSettingsDialog.cs` — планировщик автоматического переключения темы.

## Использование
Компоненты подключаются в `Form1.UI.cs` при построении интерфейса: слева размещается `DragDropPanel` + список `FileListItem`, справа TabControl с вкладками (пресеты, видео, аудио, очередь), снизу прогресс с `EstimatePanel`. Редактор видео и диалоги вызываются по кнопкам "🎬 Открыть редактор", "🔔 Уведомления", "📤 Поделиться" и событиям очереди.
