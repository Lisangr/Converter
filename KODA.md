# KODA - Контекст проекта Converter

## Обзор проекта

**Converter** — это современное настольное приложение для конвертации видео, построенное на .NET 8 и Windows Forms. Приложение использует FFmpeg для обработки видео и предоставляет богатый графический интерфейс с поддержкой пакетной обработки, очередей, пресетов и расширенных настроек.

### Ключевые технологии
- **.NET 8.0** - современная платформа с поддержкой асинхронности
- **Windows Forms** - десктопный GUI фреймворк
- **Xabe.FFmpeg** - библиотека для работы с FFmpeg
- **Microsoft.Extensions** - DI, логирование, конфигурация
- **System.Reactive** - реактивное программирование
- **Newtonsoft.Json / System.Text.Json** - сериализация данных

### Архитектурные принципы
- **Domain-Driven Design (DDD)** - четкое разделение доменной логики
- **Model-View-Presenter (MVP)** - разделение UI и бизнес-логики
- **Dependency Injection** - управление зависимостями через Microsoft.Extensions.DependencyInjection
- **Event-driven architecture** - события для связи между компонентами

## Структура проекта

### Основные компоненты

#### 📁 **Корневая директория**
- `Converter.csproj` - основной файл проекта с зависимостями
- `Program.cs` - точка входа с настройкой Host и DI
- `Form1.cs` - главное окно приложения (реализует IMainView)
- `appsettings.json` - конфигурация приложения

#### 📁 **src/Converter.Domain** - Доменный слой
**Назначение**: Бизнес-модели и правила, не зависящие от инфраструктуры
**Ключевые файлы**:
- `QueueItem.cs` - модель элемента очереди конвертации
- `ConversionSettings.cs` - настройки конвертации
- `AudioProcessingOptions.cs` - опции обработки аудио
- `AppSettings.cs` - настройки приложения

#### 📁 **src/Converter.Application** - Слой приложения
**Назначение**: Бизнес-логика, сервисы, презентеры

**Поддиректории**:
- `Abstractions/` - интерфейсы для всех сервисов
- `Services/` - реализации бизнес-логики
- `Presenters/` - MVP презентеры
- `ViewModels/` - модели представления
- `DTOs/` - объекты передачи данных

**Ключевые интерфейсы**:
- `IQueueService` - управление очередью конвертации
- `IConversionOrchestrator` - оркестрация процесса конвертации
- `IFFmpegExecutor` - выполнение FFmpeg команд
- `IThemeService` - управление темами
- `INotificationService` - система уведомлений

#### 📁 **src/Converter.Infrastructure** - Инфраструктурный слой
**Назначение**: Внешние зависимости, хранение данных, интеграции

**Поддиректории**:
- `Ffmpeg/` - интеграция с FFmpeg
- `Persistence/` - сохранение данных в JSON
- `Notifications/` - система уведомлений Windows

#### 📁 **UI/** - Пользовательский интерфейс
**Назначение**: Переиспользуемые контролы и диалоги

**Ключевые компоненты**:
- `VideoEditorForm.cs` - встроенный редактор видео
- `AudioProcessingPanel.cs` - панель настроек аудио
- `DragDropPanel.cs` - панель drag&drop для файлов
- `Controls/QueueItemsListPanel.cs` - список элементов очереди

#### 📁 **Services/** - Устаревшие сервисы
**Назначение**: Сервисы в процессе миграции в новую архитектуру
**Статус**: Постепенно переносятся в `src/Converter.Application/Services/`

## Сборка и запуск

### Требования
- Windows 10/11
- .NET 8.0 SDK (для сборки)
- Visual Studio 2022+ или Visual Studio Code
- Первое подключение к интернету для автозагрузки FFmpeg

### Команды сборки

```bash
# Восстановление зависимостей
dotnet restore

# Сборка проекта
dotnet build

# Запуск в режиме отладки
dotnet run --project Converter.csproj

# Сборка для релиза
dotnet build --configuration Release

# Публикация standalone приложения
dotnet publish -c Release -r win-x64 --self-contained
```

### Запуск через Visual Studio
1. Откройте `Converter.sln` в Visual Studio 2022+
2. Установите `Converter` как запускаемый проект
3. Нажмите F5 для запуска

## Основной функционал

### 🎬 Конвертация видео
- **Поддержка форматов**: MP4, AVI, MKV, MOV, WMV и другие
- **Кодеки**: H.264, H.265, VP9, AV1
- **Пакетная обработка**: несколько файлов одновременно
- **Настройки качества**: CRF, битрейт, разрешение

### 📋 Система очередей
- **Очередь заданий**: управление процессом конвертации
- **Приоритеты**: изменение порядка обработки
- **Пауза/возобновление**: контроль выполнения
- **Статистика**: время выполнения, размер файлов

### 🎨 Настройка интерфейса
- **Темы**: светлая/темная, кастомные цветовые схемы
- **Drag & Drop**: перетаскивание файлов в окно
- **Превью**: миниатюры и предпросмотр видео
- **Редактор**: встроенный видеоредактор

### 🔊 Обработка аудио
- **Кодеки**: AAC, MP3, FLAC, Opus
- **Эффекты**: нормализация, шумоподавление
- **Эквалайзер**: настройка частот
- **Fade эффекты**: плавные переходы

### 📱 Пресеты и профили
- **Готовые пресеты**: YouTube, Instagram, TikTok
- **Пользовательские**: создание и сохранение профилей
- **Экспорт/импорт**: обмен пресетами
- **Категоризация**: группировка по типам

### 🔔 Уведомления
- **Toast уведомления**: системные уведомления Windows
- **Звуковые сигналы**: настраиваемые звуки
- **Прогресс**: отображение хода выполнения
- **Завершение**: уведомления о готовности

## Текущее состояние разработки

### ✅ Выполненные задачи
- Базовая архитектура с разделением на слои
- MVP паттерн для UI
- Система Dependency Injection
- Базовая интеграция с FFmpeg
- Очередь конвертации
- Система тем
- Drag & Drop интерфейс

### 🔄 В процессе разработки
- **Рефакторинг DI**: устранение дублирующих ServiceProvider
- **Объединение UI**: переход на единый DataGridView
- **Оптимизация очереди**: использование нового QueueProcessor
- **FileService**: вынос файловой логики из UI

### 📋 Запланированные улучшения
- Полное тестирование всех сценариев
- Оптимизация производительности
- Расширенные настройки конвертации
- Поддержка аппаратного ускорения
- Модульные тесты

## Правила разработки

### Стиль кода
- **Язык**: Русские комментарии и документация
- **Форматирование**: Стандарты .NET (EditorConfig)
- **Именование**: PascalCase для публичных членов, camelCase для приватных
- **Async/await**: Вся асинхронная логика использует async/await

### Архитектурные принципы
- **Разделение ответственности**: каждый слой имеет четкие границы
- **Инверсия зависимостей**: зависимости через интерфейсы
- **Единственная ответственность**: классы выполняют одну задачу
- **Открытость/закрытость**: расширение через наследование, изменение через переопределение

### Рекомендации по разработке
1. **Новые сервисы** размещать в `src/Converter.Application/Services/`
2. **UI компоненты** создавать в `UI/Controls/`
3. **Доменные модели** хранить в `src/Converter.Domain/Models/`
4. **Интерфейсы** определять в соответствующих `Abstractions/` папках
5. **Тесты** создавать рядом с тестируемым кодом

## Типичные задачи и решения

### Добавление нового сервиса
1. Создать интерфейс в `src/Converter.Application/Abstractions/`
2. Реализовать в `src/Converter.Application/Services/`
3. Зарегистрировать в `ServiceCollectionExtensions.cs`
4. Использовать через DI в нужных компонентах

### Добавление нового UI контрола
1. Создать класс в `UI/Controls/`
2. Реализовать в `UI/Controls/YourControl.cs`
3. Добавить дизайн в `YourControl.Designer.cs`
4. Интегрировать в основную форму

### Работа с очередью конвертации
1. Использовать `IQueueRepository` для чтения данных
2. Изменения через методы репозитория
3. UI обновляется автоматически через события
4. Обработка происходит в `IQueueProcessor`

### Отладка FFmpeg операций
1. Проверить путь к FFmpeg в `appsettings.json`
2. Использовать `FFmpegExecutor` для команд
3. Логи смотреть в выводе приложения
4. Тестировать команды вручную через консоль

## Часто используемые файлы

### Конфигурация
- `appsettings.json` - основные настройки приложения
- `Properties/Settings.settings` - пользовательские настройки
- `Presets/*.xml` - файлы пресетов

### Данные приложения
- `%LocalAppData%/Converter/` - рабочая директория
- `%LocalAppData%/Converter/ffmpeg/` - установленный FFmpeg
- `queue.json` - сохранение очереди заданий
- `settings.json` - пользовательские настройки

### Логи и отладка
- Логи выводятся в консоль приложения
- FFmpeg команды логируются с параметрами
- Ошибки показываются через `INotificationService`

## Безопасность и производительность

### Управление памятью
- Все `IDisposable` ресурсы освобождаются корректно
- Фоновые задачи отменяются при закрытии
- Использование `CancellationToken` для контроля операций

### Обработка ошибок
- Все асинхронные операции обернуты в try/catch
- Пользовательские ошибки показываются через UI
- Критические ошибки логируются

### Производительность
- Пакетная обработка файлов
- Использование каналов для очереди
- Асинхронные операции для UI
- Кэширование пресетов и настроек

## Получение справки

### Документация
- `README.md` - основная документация проекта
- `REFACTORING_PLAN.md` - план текущих изменений
- `TECHNICAL_DOCUMENTATION.md` - техническая документация

### Исходный код
- Комментарии в коде на русском языке
- XML документация для публичных API
- Примеры использования в тестах

### Отладка
- Включить подробное логирование в `appsettings.json`
- Использовать отладчик Visual Studio
- Проверять вывод FFmpeg команд

---

**Последнее обновление**: Ноябрь 2025  
**Версия**: 1.0.0  
**Статус**: Активная разработка